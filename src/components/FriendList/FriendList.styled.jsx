import styled from '@emotion/styled';

export const StyledFriendsItem = styled.ul`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(170px, auto));
  gap: 0px;

  width: 80%;
  padding: 0;
  margin-left: auto;
  margin-right: auto;
`;
